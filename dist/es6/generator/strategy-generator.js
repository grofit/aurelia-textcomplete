export class StrategyGenerator
{
    createStrategy(match, search, replace, index, template, cache, context, idProperty){}

    getDefaultReplace(token) {};

    getDefaultMatch(token) {};

    getDefaultTemplate() {};
}